# No chat prompt — Sarvam Vision's Document Intelligence job API is configured
# via job_parameters (language, output_format), not a system prompt. See
# app/prompts/document.md for the extraction/normalization contract.
