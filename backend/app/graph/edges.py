# Phase 5's graph is a plain linear chain (START -> supervisor -> policy ->
# END), so no conditional routing function is needed yet. Real branching
# (e.g. document-dependent gating) arrives once downstream agents need it.
